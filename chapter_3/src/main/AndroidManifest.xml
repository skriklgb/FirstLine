<?xml version="1.0" encoding="utf-8"?>
<manifest package="skrik.lgb.chapter_3"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".widget.WidgetActivity">
        </activity>
        <activity android:name=".layout.LinearLayoutActivity">
        </activity>
        <activity android:name=".custormwidget.CustomViewsActivity">
        </activity>
        <activity android:name=".ListView.ListViewActivity">
        </activity>
        <activity android:name=".ListView.MyListviewActivity">

        </activity>
        <activity android:name=".ChatView.ChatViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>